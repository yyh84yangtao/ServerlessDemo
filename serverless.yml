component: express
name: express-UlxeN5P__
org: '1251891378'
app: express-demo-git
stage: dev
inputs:
  src:
    src: ./
    exclude:
      - .env
      - node_modules/**
  region: ap-guangzhou
  runtime: Nodejs10.15
  apigatewayConf:
    protocols:
      - http
      - https
    environment: release
    serviceTimeout: 60
    autoCreateDns: false
  isAutoCiDeploy: false
  functionConf:
    eip: false
    timeout: 3
    memorySize: 256
  layers:
    - name: '${output:${stage}:${app}:express-demo-git-layer.name}'
      version: '${output:${stage}:${app}:express-demo-git-layer.version}'
